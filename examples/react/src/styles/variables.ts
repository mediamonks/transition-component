// eslint-disable-next-line no-shadow
export enum TransitionDuration {
  Fast = '100ms',
  Normal = '200ms',
  Slow = '300ms',
}

// eslint-disable-next-line no-shadow
export enum FontWeight {
  Normal = 300,
  Medium = 500,
  SemiBold = 600,
  Bold = 700,
}

// eslint-disable-next-line no-shadow
export enum FontFamily {
  Arial = 'Arial, Helvetica, sans-serif',
  SystemNeue = '-apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Helvetica Neue,Arial,sans-serif',
}
